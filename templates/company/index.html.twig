{% extends "base.html.twig" %}

{% block body %}
    <body>
        <h1> Welcome! </h1>
        {% if form is defined and form is not null %}
            <h1> {{ form(form) }}</h1>
        {% endif %}
    </body>
{% endblock %}